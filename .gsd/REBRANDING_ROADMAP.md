# Roadmap: Rebranding & Stabilization

## Phase 1: Context & Rule Sync
- Synchronize `firestore.rules` completely.
- Search for all occurrences of "Halo Bus" to estimate impact.
- Identify the source of the `invalid-api-key` error.

## Phase 2: Global Rebranding
- Execute search-and-replace for "Halo Bus" across the codebase.
- Verify mobile app strings in `strings.xml` or `pubspec.yaml` (if applicable).
- Update metadata in `index.html`.

## Phase 3: Firebase Resolution
- Fix the API key in the frontend configuration files.
- Verify the "Something went wrong" screen is replaced by the actual login UI.

## Phase 4: Verification
- Build and test the portals.
- Final build of the Mobile APK.

